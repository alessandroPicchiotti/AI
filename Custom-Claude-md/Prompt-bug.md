1)  Prima di cambiare qualunque cosa: elenca ogni possibile causa radice di questo bug, classificale per probabilità, 
e mostrami la linea di evidenza specifica per ognuna. Poi dimmi quali devono TUTTE essere fixate perché il sintomo scompaia. Aspetta il mio via prima di editare.

2) Rimani in plan mode. Produci un piano come lista task numerata dove ogni item nomina il percorso file esatto, metodo/classe,
   e range linea approssimativo, più quale layer architetturale lo possiede (view / controller / repository / entity / SQL). Mantieni questa lista in un file così possiamo spuntare elementi attraverso la sessione.
